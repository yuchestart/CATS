/**
 *  Just a bunch of keywords to let the library know what it's doing. Not much else.
 */
const glDictionary = {
    TRIANGLE_STRIP:0,
    TRIANGLES:1,
    POINT_CLOUD:2,
    ATTRIBUTE:3,
    UNIFORM:4,
    NONATTRIBUTE:5,
    ARRAYS:6,
    ELEMENTS:7,
    USES_COLOR_BUFFER:8,
    USES_TEXTURE_BUFFER:9,
    USES_NO_BUFFER:10,
}